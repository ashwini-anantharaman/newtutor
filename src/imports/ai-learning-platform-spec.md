I want to create an AI enabled learning platform similar to khan academy and study fetch. We came up with our own UI designs attached to this message. Instructor workflow part 1 grouping has the following frames : First a login page that has start learning and educator buttons on it along with an animation of our logo. We also want the full UI to match the UI on the “interactive mode” the “summary mode” and the “narrative mode”. The login page goes to a teacher login, and a student login. We will start with the teacher login first with the necessary login details. Then there is teacher onboarding question 1 through teacher onboarding question 3. I want you to use the same questions. Then there is a upload materials page where it takes a pdf, a quizlet, audio, etc (add the same ones that were on study  fetch) for context and embeddings. The generate button goes to the main building page for the instructor where they can add “+” text, flashcards, animations to any number by clicking the + icon on top of each. The embeddings will be generated with blocks chosen by default. The instructor can click the next arrow and do the same for the generated unit 1 , 2, till unit x units. Then the instructor can generate the final product. The final product has a side bar (only available for instructor mode obviously) and it has the text, embeddings, animations flashcards blocks where you can either manually edit each block our use an ai chat to edit any field. An icon on the left of the chat tells which feature it is focusing on while editing. The teacher can keep clicking the next unit button till all the units are edited to their desire to publish it where it will route back to the course's homepage. This all is entailed in the lessons button on the instructor sidebar. The instructor dashboard has a detailed account of each student to see student mastery, understanding and progress, and the AI has to look at each to make suggestions at the bottom of the page which the instructor can review and change if they like it by clicking the button. The challenge details button on the sidebar leads you to a new page that has three tabs “students registered, info, and add teachers”. On the student registered tab the instructor can manually add students and generate a student classroom join link and a classroom code. The info tab has the name of the challenge, the details of the challenge, or any fields that matter. The add teachers tab has a teacher classroom join link for other teachers to join under the organization.The settings button on the instructor sidebar has two tabs, the organization details, and the AI prompts. The organization details just has the simple login details and availability to change password etc, and the AI prompts tabs has all the current AI prompts per grade level in the backend but are editable so it can regenerate based on the new prompts. Okay that was the whole instructor workflow. You can change any formatting to best fit your needs and bet fits the UI design. You can add any buttons that are necessary for routing back and forth and that are missing on the frames.   Now for the student workflow, once a student clicks start learning, they are taken to the login page and met with onboarding questions. Make sure the onboarding questions are the same. They are taken to their courses homepage, where when they click the + button you can join the link or join with classroom code. Once the student clicks join they are met witht the student interface. This interface has a sidebar with lessons, student dashboard, challenge details, tools, and settings. Lessons entails the full lessons by unit that the teacher generated.The student can change the format of the units to interactive, summary, or narrative modes by wish of the click, and the student can also ask questions on the page in the AI chat. The student can go to the next unit till the end. Remember each unit can be text, embeddings, animations, flashcards, small quiz text,anything that the instructor generated on their end. For the summary mode we want newspaper format, for the narrative mode we want the same format as the summary mode frame, and for the interactive mode we want a text messaging system as the UI only for the text but everything remains the same as generated in newspaper format. The student dashboard tab takes you back to the course page with a progress bar on it. The challenge details tab takes you to the timer countdown to the challenge, the course mastery , and a button for the practice material. This practice material routes to the tools tab. The tools tab once you click on it has a small pop up with 5 different tools : view quizzes, view tests, view flashcards, audio, notes. View quizzes should show all of the quizzes compiled in unit 1 , unit 2, and the ones that weren’t done yet should be locked. There should be a generate more button on the bottom right to create more quizzes and do them.  View tests should show all of the tests compiled in unit 1 , unit 2, and the ones that weren’t done yet should be locked. There should be a generate more button on the bottom right to create more tests and do them.  View flashcards should show all of the flashcards compiled in unit 1 , unit 2, and the ones that weren’t done yet should be locked. There should be a generate more button on the bottom right to create more flashcards and do them.  The audio tab should have the separate video embeddings per unit with a transcript on the right and different pitstops at the timebar to ask pop up questions. There should be an ai chat on the bottom right to ask questions and any point of the video to ask questions and it will answer and reference an appropriate timestamp. THe notes tab should have the notes per unit and a chat bot on the right to ask questions about the notes. The next button should go to the next unit and etc.  The quizzes, the tests, and the flashcards should follow a Bayesian Knowledge Tracing learning style, follow the UI. When the full course is done the certificate should be generated in the challenge details tab. The settings tab should have accessibility settings, font size, deaf settings, color blind settings, etc whatever necessary.You can change any formatting to best fit your needs and bet fits the UI design. You can add any buttons that are necessary for routing back and forth and that are missing on the frames. 