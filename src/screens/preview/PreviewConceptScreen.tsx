import { PreviewConceptLesson } from "./PreviewConceptLesson";

/** Full-screen instructor preview of the 7-step student lesson flow. */
export function PreviewConceptScreen() {
  return <PreviewConceptLesson />;
}
